# Backend
PORT=3101
# Comma-separated allowlist. Use * in dev to avoid issues when Vite changes ports.
CORS_ORIGIN=*

# LLM
OPENAI_API_KEY=your_key_here
OPENAI_MODEL=gpt-5-nano
# Used when the Responses API returns no user-visible text (or when gpt-5 access is limited)
OPENAI_FALLBACK_MODEL=gpt-4o-mini

# DB (Neon / Postgres)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require
